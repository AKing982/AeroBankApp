import {Paper, Table, Typography} from "@mui/material";

export default function MonitoringJobs()
{
    return (
        <Paper>
            <Typography variant="h6">Monitoring Logs</Typography>
            <Table>
                {/* Table structure similar to JobManagement, but with log details */}
            </Table>
        </Paper>
    );
}